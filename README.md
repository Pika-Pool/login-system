# login-system
### run application:
- clone the repo,
- In your terminal, run the following commands
```sh
$ cd login-system
$ npm install
$ npm start
```
and open http://localhost:3000/ in your browser

### run as developer:
- clone repo
- In your terminal, run the following commands
```sh
$ cd login-system
$ npm install
$ npm run dev
```
